lsblk (list block devices)
apt upgrade
apt update 
apt install sudo (install sudo)
apt install vim (install vim)
apt install git (install git)
apt-get install wget
apt install zsh -y 
apt install ssh
apt install ufw -y

USER
adduser username sudo
getent group sudo(grupları görme)
vim /etc/sudoers (sudo yetkisi ver)
sozdamar	ALL=(ALL:ALL) ALL
sudo -v (sudo yetkisini al)
compgen -u (bütün kullanıcıları gör)

SSH
dpkg -l | grep ssh (dpkg -l (debian paket yöneticisi), grep ssh(ssh'ı bul))
sudo sytemctl status ssh (ssh durumunu kontrol et)
sudo service restart ssh
sudo vim /etc/ssh/sshd_config (portları düzenle)
sudo grep Port /etc/ssh/sshd_config

FIREWALL
sudo ufw enable
sudo ufw allow ssh
sudo ufw allow 4242
sudo ufw status numbered
sudo ufw delete <rule_number>
ssh sozdamar@127.0.0.1 -p 4242 (ana makine terminalinden sanal makineye bağlan)
logout/exit (çıkış yap)

hostnamectl
hostnamectl set-hostname server1 (makine adını değiştir)
vim /etc/hosts (adı değiştir)

SUDO
sudo vim /etc/sudoers
(parola değişiklikleri sudo için)

PASSWORD
sudo vim /etc/login.defs (min-max ayarlama)

sources:
https://rphlr.github.io/42-Evals/
